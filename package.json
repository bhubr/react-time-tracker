{
  "name": "node-react-timetracker-monorepo",
  "version": "0.0.1",
  "main": "index.js",
  "author": "Benoit Hubert <benoithubert@gmail.com>",
  "license": "MIT",
  "scripts": {
    "lint": "lerna run lint --stream",
    "start:dev": "lerna run start:dev --stream",
    "start": "lerna run --scope react-time-tracker-back start --stream",
    "build": "lerna run build --stream",
    "postinstall": "pnpm run build"
  },
  "devDependencies": {
    "husky": "^3.0.4",
    "lerna": "^3.16.4"
  },
  "husky": {
    "hooks": {
      "pre-commit": "npx lerna run --concurrency 1 --stream precommit"
    }
  },
  "engines": {
    "pnpm": "^4.6.0",
    "node": "^10.16.3"
  }
}
