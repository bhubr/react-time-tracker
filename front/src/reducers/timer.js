// import {
// } from '../actions'


const initialState = {
}

const timerReducer = (state = initialState, action) => {
  switch(action.type) {

    default: {
      return state
    }
  }
}
export default timerReducer
